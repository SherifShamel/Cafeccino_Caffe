import 'package:flutter/material.dart';

import '../main.dart';

const int kPrimaryColor = 0xffFF902B;
double screenWidth = MediaQuery.sizeOf(navigatorKey.currentState!.context).width;
double screenHeight = MediaQuery.sizeOf(navigatorKey.currentState!.context).height;
